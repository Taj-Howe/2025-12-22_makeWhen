"use client";

import ThemeRoot from "../src/ui/ThemeRoot";
import App from "../src/ui/App";

const AppClient = () => {
  return (
    <ThemeRoot>
      <App />
    </ThemeRoot>
  );
};

export default AppClient;
